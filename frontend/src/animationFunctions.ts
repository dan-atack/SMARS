// Since the Colonists will ultimately do a large variety of activities, their animations will be kept in their own file

export const formula = (fpm: number, tick: number, sign: number) => {
    return fpm;
}